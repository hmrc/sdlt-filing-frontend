# Copyright 2022 HM Revenue & Customs
#

include "frontend.conf"

appName="sdltc-frontend"

play.http.router=prod.Routes


play.application.loader = "uk.gov.hmrc.play.bootstrap.ApplicationLoader"
play.http.requestHandler = "uk.gov.hmrc.play.bootstrap.http.RequestHandler"
play.modules.enabled += "uk.gov.hmrc.play.audit.AuditModule"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientModule"
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.graphite.GraphiteMetricsModule"

play.modules.enabled += "uk.gov.hmrc.play.bootstrap.frontend.FrontendModule"

play.http.errorHandler = "config.SDLTCErrorHandler"

play.filters.disabled += "play.filters.csrf.CSRFFilter"

play.filters.headers.contentSecurityPolicy = "base-uri 'self';block-all-mixed-content;child-src 'none';connect-src 'self' https://www.google-analytics.com;default-src 'self' 'unsafe-inline' localhost:9250 localhost:9514 localhost:12345 https://www.google-analytics.com analytics.analytics-egain.com https://www.googletagmanager.com https://tagmanager.google.com fonts.googleapis.com ssl.gstatic.com www.gstatic.com fonts.gstatic.com data:;form-action 'self';frame-ancestors 'none';script-src 'self' 'unsafe-inline' ajax.googleapis.com www.google-analytics.com cdn.optimizely.com;"

# Assets configuration
# ~~~~~
# For now just some (very short) test settings to prove it works as expected - i.e. control over the individual resources
"assets.cache./public/index.html"="public, max-age=900, s-maxage=900" # 15 minutes
"assets.cache./public/javascript/calc.js"="public, max-age=31536000" # 1 year (365 days)
"assets.cache./public/javascript/calc-templates.js"="public, max-age=31536000" # 1 year (365 days)

# Override the allowed cache types to include HTML for this application
caching.allowedContentTypes=["image/", "text/css", "application/javascript", "text/html"]


microservice {
  metrics {
    graphite {
      host = graphite
      port = 2003
      prefix = play.${appName}.
      enabled = false
    }
    name = ${appName}
    rateUnit = SECONDS
    durationUnit = SECONDS
    showSamples = true
    jvm = true
    enabled = false
    extraMeasured = []
  }
}

tracking-consent-frontend {
  gtm.container = "c"
  url = "http://localhost:12345/tracking-consent/tracking.js"
}

optimizely {
  projectId = "123456789"
}

auditing {
  enabled=false
  traceRequests=true
  consumer {
    baseUri {
      host = localhost
      port = 8100
    }
  }
}

# By default all controllers will have logging enabled.
# The below controllers are the default exceptions to this rule.

controllers {
  uk.gov.hmrc.play.health.AdminController = {
    needsLogging = false
  }

  uk.gov.hmrc.play.health.HealthController = {
    needsAuth = false
  }
}

feedback-survey-frontend {
  url="http://localhost:9514/feedback/SDLTC"
}

contact-frontend {
  protocol = https
  host = "http://localhost:9250"
  port = 443
  serviceId = "sdltc-frontend"
}

urls {
  footer = {
    cookies = "/help/cookies"
    privacy_policy = "/help/privacy"
    terms_and_conditions = "/help/terms-and-conditions"
    accessibility_statement = "accessibility-statement/calculate-stamp-duty-land-tax"
    help_page = "https://www.gov.uk/help"
  }
}

accessibility-statement-frontend {
  host = "http://localhost:12346"
  url = "/accessibility-statement"
}

accessibility-statement.service-path = "/calculate-stamp-duty-land-tax"
